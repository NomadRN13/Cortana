# 40/Love — Bug Tracker

Source: adversarial code review of the live-data swap (mobile dual-mode state
layer + Supabase API surface), 2026-08-06. Backend paths cannot run in the QA
environment, so these are traced from code against the schema in
`supabase/migrations/20260805000001_initial_schema.sql`. Bundle check passed
(esbuild, App.js + project externals, 0 errors).

Severity: **Blocker** = breaks sign-in, matching, chat, or safety. **Major** =
wrong behavior a beta tester will hit. **Minor** = edge case / divergence.
**Polish** = cosmetic.

| ID | Severity | Title | Repro / trace | Suspected area | Status |
|----|----------|-------|---------------|----------------|--------|
| B-01 | Blocker | Live chat drops the first message after a match and can fabricate a canned "reply" from the real person | `sendMessage` reads `threads` from the render closure and falls into the **demo** branch whenever the thread is missing or has no `matchId` while live (`mobile/src/state.js:450-468`, guard at :453). Two reachable paths: (1) Match Point modal → "Send a message" (`HomeScreen.js:148-152`) navigates before `refreshMatchesAndThreads` finishes — thread is `undefined`, so `appendMsg` no-ops (message vanishes from UI), nothing is sent to the backend, and the demo canned-reply `setTimeout` fires; 1.5s later the refresh has populated the real thread and the canned "Sounds great — see you on the court! 🎾" is appended as `who:'them'` — a fabricated message attributed to a real person. (2) `refreshMatchesAndThreads` calls `setMatches` (:154) before its second `await` and `setThreads` (:156) — in that window MatchesScreen's tap calls `ensureThread(id)` (`MatchesScreen.js:62`), which creates a thread **without** `matchId` (`state.js:384-386`); sends then show locally, never reach Supabase, get a canned reply, and are wiped when `setThreads` replaces threads. | mobile/src/state.js (`sendMessage`, `ensureThread`, `refreshMatchesAndThreads`) | Fixed |
| B-02 | Major | `api.swipe` match detection can return a stale/closed match and falsely fire the Match Point modal | Detection query (`mobile/src/api/backend.js:126-131`) filters `mode` + `or(user_a/user_b eq target)` but **omits `.is('closed_at', null)`** — inconsistent with `listMatches` (:141). Trace: target aces me (an ace alone creates a match — migration :222-231); the match is later closed (support/safety sets `closed_at`; no client unmatch UI exists yet). I never swiped them, so they stay in my deck with +10 ace score. I like them → swipe insert succeeds → trigger's `on conflict do nothing` creates nothing → client select finds the **closed** match → Match Point modal fires. `refreshMatchesAndThreads` excludes it, so "Send a message" lands in B-01's dropped-message path, and `messages_insert` RLS (migration :456-467) silently eats anything sent. (Third-party/wrong-pair rows are excluded by `matches_select` RLS, and `maybeSingle` is safe under the `unique (user_a, user_b, mode)` constraint — verified.) | mobile/src/api/backend.js (`swipe`) | Fixed |
| B-03 | Major | Live deck repeats the card just swiped (and can suppress its Match modal) after a report/skip; deck can stick on empty | `currentProfile` skips `blocked`/`seen` cards from `liveIndex` forward (`state.js:353-361`) but `advance` only does `liveIndex + 1` from the un-skipped index with a non-functional `setLiveIndex` (:363-372). Trace: deck [A,B,C], report A from deck (`report` sets `seen[A]`, :505-508) → B displays (index 1) while `liveIndex` stays 0 → like B → `advance` sets `liveIndex=1` → **B renders again**; liking B again throws a unique-violation on `swipes (actor, target, mode)` PK (migration :77) which `swipeLike` swallows (:413) — so if that like was the mutual one, the Match Point modal never fires. Variant: report the trailing cards → `currentProfile` returns null while `liveIndex < liveDeck.length`, so the `next >= liveDeck.length → refreshDeck` trigger never runs — "That's everyone nearby" until mode toggle or restart. | mobile/src/state.js (`advance`, `currentProfile`) | Fixed |
| B-04 | Major | Reporting a player only hides them locally — they reappear in the live deck after refresh/restart | `report` sets in-memory `seen[id]` and inserts a `reports` row (`state.js:505-508`, `backend.js:221-225`), but `get_discovery_deck` filters only blocks and swipes (migration :320-324) and `seen` is never persisted. Any `refreshDeck` (mode switch, deck exhaustion, app restart) resurfaces the reported profile to the reporter. Safety expectation: report should at least permanently hide the profile for the reporter (record a pass swipe or block alongside). | mobile/src/state.js (`report`) + deck RPC contract | Fixed |
| B-05 | Major | Demo seed data leaks into live mode: fake matches/threads/notifications shown to signed-in users | `matches` initializes to `['maya','sam','priya']`, `threads` to demo `THREADS`, `notifs` to demo `NOTIFICATIONS` (`state.js:100-104`) regardless of mode. On live cold start these render (Matches grid, Chat list, bell panel — `profileById` falls back to demo `PROFILES`, :382) until `refreshMatchesAndThreads` succeeds; its `catch` keeps current state (:162), so **offline the fake people persist indefinitely** and chatting with them yields demo canned replies while the user believes they're real. Live state should initialize empty. | mobile/src/state.js (initial state + `refreshMatchesAndThreads` fallback) | Fixed |
| B-06 | Major | Pair matched in two modes → duplicate thread/match entries under the same id, cross-posted local messages | Matching in both `date` and `play` is normal (deck re-serves a matched user in the other mode — swipe exclusion is per-mode, migration :321-324; `unique (user_a, user_b, mode)` allows both rows). `refreshMatchesAndThreads` maps each match row to `id = otherId` with no dedupe (`state.js:151-160`): `matches` lists the person twice (duplicate React keys in MatchesScreen grid), ChatList shows two rows with the same `keyExtractor` id, `appendMsg`/`markRead`/`respondCourt` mutate **both** threads (`.map` on `t.id === id`, :446-448), and `sendMessage`/`subscribeThread` always pick the first (newest) match — the older mode's thread is unreachable for sending yet still displayed. | mobile/src/state.js (`refreshMatchesAndThreads`, thread keying by user id instead of match id) | Fixed (newest match wins; cross-mode thread merge deferred) |
| B-07 | Major | Realtime never delivers: no migration adds `messages` to the `supabase_realtime` publication | `subscribeToMessages` uses `postgres_changes` on `public.messages` (`backend.js:180-187`), which requires the table in the `supabase_realtime` publication. No SQL in `supabase/` mentions any publication (grep verified across both migrations + seed). On a project provisioned via `supabase db push` per the migration header, the channel subscribes cleanly but receives zero events — incoming live messages only appear after app restart. Needs one-line migration (`alter publication supabase_realtime add table public.messages;`). Verify against a real project before closing. | supabase/migrations (missing publication DDL) | Fixed (verify on real project) |
| B-08 | Major | Court-time accept/decline never reaches the proposer in live mode | `respond_court_time` mutates the existing message row's `court_payload` (migration :360-379) — an UPDATE. The only realtime subscription listens for INSERT (`backend.js:184`), and `refreshMatchesAndThreads` re-runs only on a new match or app relaunch. The proposer's card stays "proposed" forever; both players may believe different things about a real-world meetup. (Payload shape itself checks out: client `{venue, day, time, sport, status}` with lowercased sport matches the schema comment and `mapMsgRow` re-caps it — verified.) | mobile/src/api/backend.js (`subscribeToMessages` events) + state refresh strategy | Fixed |
| B-09 | Major | Profile photo upload likely fails silently on device (`fetch(file://).arrayBuffer()` in React Native) | `uploadProfilePhoto` does `fetch(localUri).then(r => r.arrayBuffer())` on an ImagePicker `file://` URI (`backend.js:95-110`). RN's fetch historically rejects `file://` bodies/`arrayBuffer` (platform-dependent); both call sites swallow errors (`state.js:236, 329` — `.catch(() => {})`), so a failure means the user thinks their photo saved and other players see initials forever. **Unconfirmed** — cannot execute device code in this environment; tried static trace only. Needs one on-device run (watch the `photos` storage bucket). Table/constraint usage (`onConflict: 'user_id,position'`, `moderation_status: 'pending'`) matches schema — verified. | mobile/src/api/backend.js (`uploadProfilePhoto`) | Mitigated (expo-file-system read; confirm on device) |
| B-10 | Minor | 18+ client gate off-by-one for negative UTC offsets (includes Indianapolis); live users get a raw DB error instead of the friendly alert | `ageFromBirthdate` parses `YYYY-MM-DD` with `new Date(...)` (UTC midnight) then compares with **local** date parts (`state.js:15-23`). In UTC-5, a birthdate of tomorrow-minus-18-years (still 17 today) computes as 18, passing the Onboarding gate (`OnboardingScreen.js:56-58`). Live, the DB constraint (migration :27) correctly rejects the upsert but the tester sees the raw check-constraint message from the generic catch (`OnboardingScreen.js:80-83`), not the "40/Love is 18+" alert. Demo mode admits them a day early. | mobile/src/state.js (`ageFromBirthdate`) | Fixed |
| B-11 | Minor | Preference updates silently diverge from the server (e.g., ageMin > ageMax) | `updatePrefs` applies locally then fires `updateMyProfile` with `.catch(() => {})` (`state.js:245-255`). Settings lets ageMin exceed ageMax (independent 18–99 checks, `SettingsScreen.js:103-118`); the DB `check (age_min <= age_max)` (migration :41) rejects the write, the client shows the new values, and server-side deck filtering keeps using the old ones. Same silent divergence for any offline pref change. | mobile/src/state.js (`updatePrefs`) + SettingsScreen validation | Fixed (client clamp; offline divergence remains) |
| B-12 | Minor | Changing prefs never refreshes the live deck | `updatePrefs` doesn't call `refreshDeck` (`state.js:245-255`) and client-side `eligible()` filtering applies only to the demo deck (:337-345, :353-361). Narrowing radius/age keeps serving now-out-of-range cards until the current 20-card batch runs out; widening shows nothing new until then either. | mobile/src/state.js (`updatePrefs`/deck lifecycle) | Fixed |
| B-13 | Minor | Event RSVP: optimistic state can diverge; spots count includes waitlist/checked-in | `toggleJoin` updates `going`/`spotsLeft` locally then `api.rsvp(...).catch(() => {})` (`state.js:520-532`) — an offline tap shows "Going ✓" for an event the server never recorded. Also `spotsLeft = capacity - event_rsvps.length` counts `waitlist`/`checked_in` rows as occupying "going" spots (:174-185 vs `rsvp_status` enum, migration :13). | mobile/src/state.js (`toggleJoin`, `refreshEvents`) | Fixed (counts only going/checked-in) |
| B-14 | Minor | Live mode never sets unread or read receipts | `refreshMatchesAndThreads` hardcodes `unread: false` (`state.js:160`); nothing writes `messages.read_at` despite the `messages_mark_read` RLS policy existing for it (migration :468-470). "YOUR SERVE"/unread dots work on heuristics only and reset on every refresh. | mobile/src/state.js + missing backend call | Open (v1.1) |
| B-15 | Minor | Saved / seen / blocked-local state lost on restart in live mode | `saved`, `seen`, `blocked` are in-memory maps (`state.js:97-99`) never persisted or hydrated from the backend (blocks ARE on the server but never re-fetched into local state). After relaunch the Saved-for-later strip is empty; local blocked filtering of matches/threads relies on the server refresh instead (works, but saved is simply gone). | mobile/src/state.js (persistence) | Fixed (saved persisted) |
| B-16 | Minor | Rapid mode switching can land the wrong deck / record swipes under the wrong mode | Each mode change fires `refreshDeck(mode)` with no request-cancellation or staleness guard (`state.js:217-220, 138-146`); out-of-order responses let a previous mode's deck render while `swipeLike` sends the **current** `mode` (:407) — a swipe recorded in a mode the card wasn't surfaced for, and possible silent unique-violations. Needs slow network + fast toggling. | mobile/src/state.js (`refreshDeck` race) | Fixed |
| B-17 | Polish | Failed deck fetch shows "That's everyone nearby" instead of an offline/error state | `refreshDeck`'s catch keeps an empty deck silently (`state.js:145`); airplane-mode sign-in reads as "you've seen all the players in range" (`HomeScreen.js:104-110`), which is false and discouraging for a launch-city beta. | mobile/src/state.js / HomeScreen empty state | Fixed |

## Round-2 findings (from writing the test plan) — 2026-08-06

| ID | Severity | Title | Repro / trace | Suspected area | Status |
|----|----------|-------|---------------|----------------|--------|
| B-18 | Major | No migration provisions the `photos` storage bucket or its policies — photo upload fails silently on a fresh project | `uploadProfilePhoto` targets `storage.from('photos')` but nothing in `supabase/` creates it; both call sites swallow errors | supabase/migrations | Fixed (20260806000003 creates bucket + own-folder policies) |
| B-19 | Major | Location never collected — radius filter passes everyone, no distances on cards, despite the v0.1 roadmap promise | No expo-location anywhere; `finishOnboarding` never sends `approxLat/approxLng`, so `miles_between` is null for all pairs | mobile (onboarding + live boot) | Fixed (coarse ~1km location at onboarding + refresh on live boot; permission-denied degrades to no distances) |
| B-20 | Polish | Demo wording shipped in live builds (sign-out alert, Settings footer) | `SettingsScreen.js` strings assumed demo mode | mobile/src/screens/SettingsScreen.js | Fixed (mode-aware copy) |
| B-21 | Minor | First liker gets no live "Match Point!" — only the second swiper sees it in the moment | No realtime subscription on `matches`/`notifications`; notifications refresh only at boot | mobile + realtime publication | Open (v1.1 — needs notifications realtime) |

## Fix pass — 2026-08-06

All Blocker/Major findings and most Minors addressed in the same-day fix
pass (state layer rewrite + backend query/subscription fixes + realtime
publication migration). Remaining open: B-14 (read receipts, v1.1) and the
on-device confirmations noted above — both belong in the first device test
round (see the TestFlight test plan when it exists).

## Verified clean (checked, no bug found)

- Enum casing: UI capitalizes for display only; every write path lowercases
  (`finishOnboarding` sports/skill, `sendMessage` court sport, mode keys are
  lowercase throughout) — matches `sport`, `skill_level`, `app_mode`,
  `swipe_action` enums.
- Column/RPC names: `get_discovery_deck(p_mode, p_limit)`,
  `respond_court_time(p_message_id, p_accept)`, `delete_account()`, profile
  pref columns (`radius_mi`, `age_min`, `age_max`, `same_sports_only`), deck
  row shape incl. `sports[].rating` ← `rating_label` — all match the schema.
- ConversationScreen subscription lifecycle: cleanup is a function on every
  path (`subscribeThread` returns `() => {}` or the channel remover), and the
  `thread && thread.matchId` dep correctly re-subscribes when `matchId`
  arrives late.
- `court_payload` shape `{venue, day, time, sport, status}` round-trips
  client ↔ schema comment ↔ `respond_court_time`.
- Bundle: esbuild compile of `mobile/App.js` with project externals — 0
  errors, 0 warnings.
